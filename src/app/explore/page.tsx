import Link from "next/link";
import {AppShell} from "@/components/app-shell";
import {createClient} from "@/lib/supabase/server";
export default async function ExplorePage(){const supabase=await createClient();const {data:decks}=await supabase.from("decks").select("id,name,description,updated_at,owner_id,cards(count)").eq("visibility","public").order("updated_at",{ascending:false}).limit(50);return <AppShell><div className="mx-auto max-w-7xl px-5 py-8 sm:px-8"><p className="text-sm text-slate-400">Community</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Public decks</h1><p className="mt-2 text-sm text-slate-500">Browse decks shared by Shyraq creators.</p><div className="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{(decks??[]).map((d:any)=><Link href={"/explore/"+d.id} key={d.id} className="rounded-2xl border border-black/[0.06] bg-white p-6 hover:border-black/10 hover:shadow-sm"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-slate-100 text-xs font-bold">S</div><h2 className="mt-5 font-semibold">{d.name}</h2><p className="mt-2 line-clamp-2 text-sm text-slate-500">{d.description||"No description"}</p><p className="mt-5 text-xs text-slate-400">{d.cards?.[0]?.count??0} cards</p></Link>)}</div></div></AppShell>}