import {AppShell} from "@/components/app-shell";
import {importCards} from "@/app/import/actions";
export default function ImportPage(){return <AppShell><div className="mx-auto max-w-3xl px-5 py-8 sm:px-8"><p className="text-sm text-slate-400">Data portability</p><h1 className="mt-1 text-3xl font-semibold">Import cards</h1><p className="mt-2 text-sm text-slate-500">Import Shyraq JSON exports or a CSV containing front/back columns.</p><form action={importCards} className="mt-8 rounded-2xl border border-black/[0.06] bg-white p-6"><input required type="file" name="file" accept=".json,.csv,text/csv,application/json" className="block w-full rounded-xl border border-slate-200 p-3 text-sm"/><button className="mt-5 rounded-xl bg-slate-950 px-5 py-2.5 text-sm font-semibold text-white">Import</button></form><div className="mt-5 rounded-2xl border border-dashed border-slate-300 bg-white p-5 text-sm text-slate-500">Supported now: Shyraq JSON and simple CSV. Anki APKG will use a dedicated parser adapter so templates, media and review history can be mapped explicitly instead of being silently discarded.</div></div></AppShell>}