import {AppShell} from "@/components/app-shell";
import {getCurrentUser,getReviewCard,getReviewPreferences} from "@/lib/supabase/queries";
import {ReviewRunner} from "@/components/review-runner";
const defaults={desired_retention:0.9,maximum_interval:36500,learning_steps:["1m","10m"],relearning_steps:["10m"],enable_fuzz:true,enable_short_term:true};
export default async function ReviewPage(){const item:any=await getReviewCard();const user=await getCurrentUser();const raw:any=await getReviewPreferences();const preferences=raw?{...defaults,...raw,learning_steps:Array.isArray(raw.learning_steps)?raw.learning_steps:defaults.learning_steps,relearning_steps:Array.isArray(raw.relearning_steps)?raw.relearning_steps:defaults.relearning_steps}:defaults;if(!item||!user)return <AppShell><div className="mx-auto max-w-2xl px-5 py-20 text-center"><h1 className="text-2xl font-semibold">Review queue is empty</h1><p className="mt-3 text-sm leading-6 text-slate-500">Create some cards first. New cards enter the FSRS schedule after their first answer.</p></div></AppShell>;return <AppShell><ReviewRunner userId={user.id} card={item.card} stateData={item.stateData} preferences={preferences}/></AppShell>}