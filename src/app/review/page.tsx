"use client";
import { AppShell } from "@/components/app-shell";
import { useState } from "react";
export default function ReviewPage(){const [revealed,setRevealed]=useState(false);return <AppShell><div className="mx-auto flex min-h-[calc(100vh-4.5rem)] max-w-4xl flex-col px-5 py-8"><div className="mb-6 flex justify-between"><div><p className="text-xs font-medium text-slate-400">Review</p><p className="mt-1 text-sm text-slate-500">0 cards due</p></div></div><div className="flex flex-1 items-center"><div className="w-full rounded-3xl border border-dashed border-slate-300 bg-white p-10 text-center"><h1 className="text-2xl font-semibold">Review queue is empty</h1><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-slate-500">Create cards first. Once cards are scheduled, this screen will use the FSRS scheduler and record append-only review events locally before syncing them.</p><button onClick={()=>setRevealed(!revealed)} className="mt-6 rounded-xl border px-4 py-2 text-sm font-medium">{revealed?"Ready":"Test interaction"}</button></div></div></div></AppShell>}