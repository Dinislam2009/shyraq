import { AppShell } from "@/components/app-shell";
import { getReviewStats } from "@/lib/supabase/queries";
export default async function StatisticsPage(){const stats=await getReviewStats();return <AppShell><div className="mx-auto max-w-7xl px-5 py-8 sm:px-8"><p className="text-sm text-slate-400">Insights</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Statistics</h1><p className="mt-2 text-sm text-slate-500">Derived from your recorded review events.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Reviews",String(stats.reviews)],["Accuracy",stats.accuracy===null?"—":stats.accuracy+"%"],["Study time",stats.studyMinutes+"m"],["Retention","FSRS"]].map(([a,b])=><div key={a} className="rounded-2xl border border-black/[0.06] bg-white p-5"><p className="text-sm text-slate-400">{a}</p><p className="mt-3 text-2xl font-semibold">{b}</p></div>)}</div></div></AppShell>}