import { notFound } from "next/navigation";
import { AppShell } from "@/components/app-shell";
export default async function DeckPage({params}:{params:Promise<{id:string}>}){const {id}=await params;if(!id)notFound();return <AppShell><div className="mx-auto max-w-7xl px-5 py-8 sm:px-8"><p className="text-sm text-slate-400">Deck</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Deck workspace</h1><p className="mt-2 text-sm text-slate-500">Deck ID: {id}</p><div className="mt-8 rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center"><h2 className="font-semibold">Ready for cards</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-slate-500">The editor and card browser will use the shared deck, card, tag and template data model.</p></div></div></AppShell>}