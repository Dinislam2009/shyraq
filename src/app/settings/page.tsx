import Link from "next/link";
import {AppShell} from "@/components/app-shell";
import {signOut} from "@/app/signout/actions";
import {AccountDangerZone} from "@/components/account-danger-zone";
export default function SettingsPage(){return <AppShell><div className="mx-auto max-w-4xl px-5 py-8 sm:px-8"><p className="text-sm text-slate-400">Account</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Settings</h1><p className="mt-2 text-sm text-slate-500">Account, review, sync and data controls.</p><div className="mt-8 space-y-3"><Setting title="Account" text="Email and profile" /><Setting title="Review" text="FSRS scheduler defaults and review behavior" /><Setting title="Sync" text="Offline changes and device synchronization" /><Link href="/export" className="block rounded-2xl border border-black/[0.06] bg-white p-5 hover:bg-slate-50"><p className="font-semibold">Data portability</p><p className="mt-1 text-sm text-slate-400">Import, export and backups</p></Link></div><AccountDangerZone/><form action={signOut} className="mt-8"><button className="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold">Sign out</button></form></div></AppShell>}
function Setting({title,text}:{title:string;text:string}){return <div className="rounded-2xl border border-black/[0.06] bg-white p-5"><p className="font-semibold">{title}</p><p className="mt-1 text-sm text-slate-400">{text}</p></div>}