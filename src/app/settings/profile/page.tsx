import {AppShell} from "@/components/app-shell";
import {createClient} from "@/lib/supabase/server";
import {updateProfile} from "@/app/settings/profile/actions";
export default async function ProfileSettingsPage(){
 const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return null;
 const {data:profile}=await supabase.from("profiles").select("username,display_name,bio,avatar_url,show_activity,show_followers").eq("id",user.id).maybeSingle();
 return <AppShell><div className="mx-auto max-w-2xl px-5 py-8 sm:px-8"><p className="text-sm text-slate-400">Account</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Profile</h1><p className="mt-2 text-sm text-slate-500">Control the identity shown on public decks.</p><form action={updateProfile} className="mt-8 rounded-2xl border border-black/[0.06] bg-white p-6 space-y-5"><label className="block"><span className="text-sm font-medium">Display name</span><input name="display_name" defaultValue={profile?.display_name??""} maxLength={80} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm"/></label><label className="block"><span className="text-sm font-medium">Username</span><input name="username" defaultValue={profile?.username??""} maxLength={30} placeholder="your_username" className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm"/></label><label className="block"><span className="text-sm font-medium">Avatar URL</span><input name="avatar_url" defaultValue={profile?.avatar_url??""} maxLength={500} placeholder="https://…" className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm"/></label><label className="block"><span className="text-sm font-medium">Bio</span><textarea name="bio" defaultValue={profile?.bio??""} maxLength={280} rows={5} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-3 text-sm"/></label><div className="grid gap-3 sm:grid-cols-2"><label className="flex items-center gap-3 rounded-xl bg-slate-50 p-3 text-sm"><input type="checkbox" name="show_activity" defaultChecked={profile?.show_activity!==false} className="h-4 w-4"/><span>Show creator activity publicly</span></label><label className="flex items-center gap-3 rounded-xl bg-slate-50 p-3 text-sm"><input type="checkbox" name="show_followers" defaultChecked={profile?.show_followers!==false} className="h-4 w-4"/><span>Show follower count publicly</span></label></div><p className="text-xs text-slate-400">Account email: {user.email}</p><div className="flex justify-end"><button className="rounded-xl bg-slate-950 px-5 py-2.5 text-sm font-semibold text-white">Save profile</button></div></form></div></AppShell>;
}