import {createClient} from "@/lib/supabase/server";
function esc(v:string){return '"'+v.replaceAll('"','""')+'"';}
export async function GET(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return new Response("Unauthorized",{status:401});const {data}=await supabase.from("cards").select("id,deck_id,kind,content,sort_order,created_at,updated_at").order("created_at");const lines=["id,deck_id,kind,front,back,sort_order,created_at,updated_at",...(data??[]).map((c:any)=>[c.id,c.deck_id,c.kind,c.content?.front??"",c.content?.back??"",c.sort_order,c.created_at,c.updated_at].map((v:any)=>esc(String(v??""))).join(","))];return new Response(lines.join("\n"),{headers:{"Content-Type":"text/csv;charset=utf-8","Content-Disposition:'attachment; filename="shyraq-cards.csv"'}})}